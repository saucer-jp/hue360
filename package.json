{
  "name": "hue360",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "serve -s dist -l tcp://0.0.0.0:${PORT:-3000}",
    "generate:munsell": "node scripts/generate-munsell-fixed-colors.js",
    "test": "node --test",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "culori": "^4.0.2",
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "vite": "^8.0.8"
  }
}
